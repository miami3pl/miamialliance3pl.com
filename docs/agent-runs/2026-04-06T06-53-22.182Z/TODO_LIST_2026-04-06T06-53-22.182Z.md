# TODO LIST (2026-04-06T06:53:22.182Z)

- [x] [CODEX] Author initial TODO list for this deployment/restart request.
  Revisit: OPUS-first authoring was requested by protocol, but this live run is executing directly in the active CODEX session.
- [ ] [CODEX] Capture current repo deploy state, Symbio PM2 health, active queue state, and disk headroom.
  Revisit: Recheck after restart so stale pre-restart signals are not treated as fresh blockers.
- [ ] [CODEX] Run project validation before deployment.
  Revisit: Repeat validation if deploy inputs or Firebase targets change during the run.
- [ ] [CODEX] Git commit check.
  Revisit: Mark `N/A` only if no task-relevant repo source changes were required for this run.
- [ ] [CODEX] Git push check.
  Revisit: Mark `N/A` only if no task-relevant commit was required for this run.
- [ ] [CODEX] Deploy the current Miami3PL Firebase state.
  Revisit: If deploy partially succeeds, rerun only the failed Firebase target after reviewing the Firebase CLI output.
- [ ] [CODEX] Restart the Symbio runtime stack (`symbio`, `symbio-autoheal`, `symbio-foreman`, `auth-monitor`) and confirm a clean boot.
  Revisit: If auth or bridge readiness fails after restart, inspect the freshest `AUTH_*`, `BRIDGE_READY`, and send-failure lines first.
- [ ] [CODEX] Clear the live Symbio command queue at `/Users/yuri/symbio-data/command-queue` and confirm no active backlog remains.
  Revisit: Remove only active payloads from the live queue path; leave historical artifacts elsewhere untouched.
- [ ] [CODEX] Check for fresh post-restart runtime errors and fix active blockers if they recur.
  Revisit: Treat only post-restart `FATAL`, `Unhandled rejection`, auth-refresh failures, quota errors, filesystem failures, or queue-processing failures as open blockers.
- [ ] [CODEX] Live verification from PM2 status, fresh logs, queue state, and live endpoint probes.
  Revisit: Confirm the runtime remains online after restart and the deployed site/functions answer from production before closing the run.
