# TODO LIST (2026-04-06T07:44:28.148Z)

- [x] [OPUS] Author the initial TODO list for this deploy/restart/queue-clear request.
  Revisit: OPUS-first authoring was required by protocol, but the live run was already routed into the active CODEX lane, so this checklist preserves the required execution trail.
- [x] [CODEX] Capture current repo deploy state, Firebase target config, PM2 health, queue state, and fresh Jorge/Symbio logs.
  Revisit: Re-check after deploy and restart so only post-action errors remain open.
- [x] [CODEX] Run deployment validation before touching production.
  Revisit: Repeat only if deploy inputs or Firebase auth context changes during this run.
- [ ] [CODEX] Fix any active blocker that would break deploy, restart, queue handling, or live health.
  Revisit: Prioritize current `FATAL`, `Unhandled rejection`, `SQLITE_FULL`, `ENOSPC`, auth-monitor boot failures, and queue-processing failures.
- [ ] [CODEX] Review git scope and decide whether this run requires a new commit.
  Revisit: Do not stage generated Firebase cache files, historical run artifacts, or unrelated workspace changes.
- [ ] [CODEX] Create a git commit for repo changes required by this run.
  Revisit: Mark skipped only if no repo changes are needed beyond runtime operations and run artifacts.
- [ ] [CODEX] Push the relevant branch state to the tracked remote.
  Revisit: Mark skipped only if no new commit is created during this run.
- [ ] [CODEX] Deploy the current Miami3PL Firebase project to production.
  Revisit: If the deploy partially fails, rerun only the failed Firebase target after reading the exact CLI output.
- [ ] [CODEX] Restart `symbio`, `symbio-autoheal`, `symbio-foreman`, and `auth-monitor`, then confirm a clean boot.
  Revisit: Inspect the newest `BRIDGE_READY`, auth, and fatal-error lines first.
- [ ] [CODEX] Clear the live Symbio command queue at `/Users/yuri/symbio-data/command-queue` and confirm no active backlog remains.
  Revisit: Remove only active payloads from the live queue path; leave historical artifacts elsewhere untouched.
- [ ] [CODEX] Run post-deploy validation and process health checks after restart.
  Revisit: Confirm Firebase endpoints, PM2 status, fresh logs, and queue state rather than relying on earlier output.
- [ ] [CODEX] Perform live verification before closing the run.
  Revisit: Confirm the deployed app and restarted Symbio stack are responding from the live environment.
