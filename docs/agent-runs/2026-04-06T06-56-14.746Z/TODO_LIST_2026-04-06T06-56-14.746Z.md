# TODO LIST (2026-04-06T06:56:14.746Z)

- [x] [CODEX] Author initial TODO list for this deploy/restart/queue-clear request.
  Revisit: OPUS-first authoring was requested by protocol, but this live Jorge run routed to CODEX after OPUS auth-routing backoff in the active Symbio session.
- [ ] [CODEX] Capture current repo deploy state, Firebase target config, PM2 process health, queue state, and disk headroom.
  Revisit: Recheck after restart so only fresh post-restart failures remain open.
- [ ] [CODEX] Run project validation before deployment.
  Revisit: Repeat validation if deploy inputs or Firebase targets change during this run.
- [ ] [CODEX] Review task-relevant repo changes and determine whether a new git commit is required.
  Revisit: Mark `N/A` only if no repo source changes were needed beyond runtime operations and run-note artifacts.
- [ ] [CODEX] Deploy the current Miami3PL Firebase project to production.
  Revisit: If the deploy partially fails, rerun only the failed Firebase target after reading the exact CLI error.
- [ ] [CODEX] Restart `symbio`, `symbio-autoheal`, `symbio-foreman`, and `auth-monitor`, then confirm a clean boot.
  Revisit: Inspect only fresh post-restart logs if auth, bridge, queue, or provider readiness fails.
- [ ] [CODEX] Clear the live Symbio command queue at `/Users/yuri/symbio-data/command-queue` and confirm no active backlog remains.
  Revisit: Remove only active queue payloads; leave historical artifacts outside the live queue path untouched.
- [ ] [CODEX] Trace and fix any fresh runtime, auth, or queue-processing errors surfaced after restart.
  Revisit: Prioritize `FATAL`, `Unhandled rejection`, auth refresh failures, queue write failures, provider runner failures, and filesystem errors.
- [ ] [CODEX] Push any required git commit to the tracked remote branch.
  Revisit: Mark `N/A` only if no new commit is created in this run.
- [ ] [CODEX] Run validation against the deployed state, including process health and live endpoint checks.
  Revisit: Confirm production site/functions answer after deploy and the PM2 stack remains online.
- [ ] [CODEX] Complete deployment confirmation for the intended production revision.
  Revisit: Confirm the live Firebase project accepted the intended deploy target set.
- [ ] [CODEX] Verify live behavior after deploy/restart/queue clear before closing the run.
  Revisit: Do not mark complete until fresh logs, queue checks, and live probes are clean.
