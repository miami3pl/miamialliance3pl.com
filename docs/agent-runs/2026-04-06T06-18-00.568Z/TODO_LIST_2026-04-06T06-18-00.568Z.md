# TODO LIST (2026-04-06T06:18:00.568Z)

- [x] [CODEX] Author initial TODO list for this deploy/restart request.
  Revisit: OPUS-first authoring was requested by protocol, but this active Jorge run was routed directly to CODEX under the current Symbio auth-routing state.
- [x] [CODEX] Capture current repo deploy state, Symbio PM2 health, queue state, and recent Jorge/Symbio trace evidence.
  Revisit: Recheck immediately after restart so pre-restart noise is not treated as a fresh blocker.
- [ ] [CODEX] Run project validation before deployment.
  Revisit: Repeat validation if deploy inputs or Firebase target selection changes during the run.
- [ ] [CODEX] Git commit check.
  Revisit: Mark `N/A` only if no task-relevant repo changes were required for this run.
- [ ] [CODEX] Git push check.
  Revisit: Mark `N/A` only if no task-relevant commit was required for this run.
- [ ] [CODEX] Deploy the current Miami3PL Firebase state.
  Revisit: If deploy partially succeeds, rerun only the failed Firebase target after reviewing the Firebase CLI output.
- [ ] [CODEX] Restart the Symbio runtime stack (`symbio`, `symbio-autoheal`, `symbio-foreman`, `auth-monitor`) and confirm a clean boot.
  Revisit: If auth or bridge readiness fails after restart, inspect the freshest `AUTH_*`, `BRIDGE_READY`, and `BRIDGE_ORCH_ERR` lines first.
- [ ] [CODEX] Clear the live Symbio command queue and confirm no active backlog remains.
  Revisit: Remove only active payloads from `/Users/yuri/symbio-data/command-queue`; leave history in other locations untouched.
- [ ] [CODEX] Check for fresh post-restart runtime errors and fix active blockers if they recur.
  Revisit: Treat only post-restart `FATAL`, `Unhandled rejection`, auth refresh failures, quota failures, filesystem failures, or queue-processing failures as open blockers.
- [ ] [CODEX] Live verification from PM2 status, fresh logs, queue state, and production endpoint probes.
  Revisit: Confirm the runtime stays online after restart and deployed hosting/functions answer from production before closing the run.
